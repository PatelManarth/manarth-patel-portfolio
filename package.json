{
  "name": "manarth-patel-portfolio-v2",
  "private": true,
  "type": "module",
  "scripts": {
    "content:audit": "node scripts/content-audit.mjs",
    "dev": "node scripts/content-audit.mjs && node scripts/prepare-public.mjs && astro dev",
    "build": "node scripts/content-audit.mjs && node scripts/prepare-public.mjs && astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "node scripts/content-audit.mjs && node scripts/prepare-public.mjs && astro check"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "pagefind": "^1.3.0",
    "typescript": "^5.7.0"
  }
}
